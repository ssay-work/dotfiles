- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.config/nvim: config/nvim
    ~/.profile: profile
    ~/.bashrc: bashrc
    ~/.bash_aliases: bash_aliases
    ~/.bash_env: bash_env

- create:
    - ~/tmp

- shell:
  - [./config/nvim/install_vim_plug.sh, Installing VimPlug]

