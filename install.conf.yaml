- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.config/nvim: config/nvim
    ~/.profile: profile
    ~/.bashrc: bashrc

- create:
    - ~/tmp

- shell:
  - [./config/nvim/install_vim_plug.sh, Installing VimPlug]

